<h3><code>ui-buttons</code></h3>

<h3>Buttons Component</h3>
<desc>
  The primary "grouping" components, it groups buttons together by wrapping them in the <code>div</code> of class <em>btn-group</em>. This naming convention is consistent with Bootstrap and leverages the CSS that comes with CSS to make everything look right. In a broader fashion, this component is about bringing "together" button widgets (of various types) that contextually are related and allowing them to behave in a coordinated fashion as well as visually to present themselves as a group entity.
</desc>
<desc>
  <note>Note: this component is still a work in progress, feel free to contribute with a PR.</note>
</desc>

<h3>Basics</h3>
<hr>
<div class="row">
  <div class="col-xs-4 col-sm-3 col-lg-2">
    {{#ui-buttons}}
      {{ui-button title='foo'}}
      {{ui-button title='bar'}}
      {{ui-button title='baz'}}
    {{/ui-buttons}}
  </div>
  <div class="col-xs-4 col-sm-4 col-lg-5 monospace">
  <b>\{{#ui-buttons}}</b><br/>
    &nbsp;&nbsp;\{{ui-button title='foo'}}<br/>
    &nbsp;&nbsp;\{{ui-button title='bar'}}<br/>
    &nbsp;&nbsp;\{{ui-button title='baz'}}<br/>
  <b>\{{/ui-buttons}}</b>
  </div>
  <div class="col-xs-4 col-sm-4 col-lg-5">
    Illustrates the most basic usage of this component. Basically all you're getting here is some additional bootstrap styling which rounds the corners in a pleasing way.
  </div>
</div>
<hr>
<div class="row">
  <div class="col-xs-4 col-sm-3 col-lg-2">
    {{#ui-buttons howMany=howMany disabled=isDisabled as |group|}}
      {{ui-button title='foo' group=group}}
      {{ui-button title='bar' group=group}}
      {{ui-button title='baz' group=group}}
    {{/ui-buttons}}<br/>
    # registered: {{howMany}}
  </div>
  <div class="col-xs-4 col-sm-4 col-lg-5 monospace">
  <b>\{{#ui-buttons howMany=howMany as |group|}}</b><br/>
    &nbsp;&nbsp;\{{ui-button title='foo' group=group}}<br/>
    &nbsp;&nbsp;\{{ui-button title='bar' group=group}}<br/>
    &nbsp;&nbsp;\{{ui-button title='baz' group=group}}<br/>
  <b>\{{/ui-buttons}}</b><br/>
  # registered: \{{howMany}}
  </div>
  <div class="col-xs-4 col-sm-4 col-lg-5">
    When a button detects its <code>group</code> property has been set then it uses this to register itself
    with the group. Registration allows the <code>ui-buttons</code> component to orchestrate certain things like
    sizing, enablement/disablement, etc.<br/>
    <p class="downer">
      {{ui-toggle-button on='enable the group' off='disable the group' value=isDisabled}}
    </p>
  </div>
</div>

<hr>

<div class="row">
  <div class="col-xs-4 col-sm-3 col-lg-2">
    {{#ui-buttons as |group2|}}
      {{ui-radio-button title='foo' group=group2}}
      {{ui-radio-button title='bar' group=group2}}
      {{ui-radio-button title='baz' group=group2}}
    {{/ui-buttons}}
  </div>
  <div class="col-xs-4 col-sm-4 col-lg-5 monospace">
  <b>\{{#ui-buttons as |group2|}}</b><br/>
    &nbsp;&nbsp;\{{ui-radio-button title='foo' group=group2}}<br/>
    &nbsp;&nbsp;\{{ui-radio-button title='bar' group=group2}}<br/>
    &nbsp;&nbsp;\{{ui-radio-button title='baz' group=group2}}<br/>
  <b>\{{/ui-buttons}}</b><br/>
  </div>
  <div class="col-xs-4 col-sm-4 col-lg-5">
    Up to now we were just using the regular old <code>ui-button</code> button but more often then not when're grouping buttons we're going to want to use either a <em>radio</em> or <em>checkbox</em> styled button which contains some "state". In the case of a
    radio button, you can see that only one button at a time can be picked.
  </div>
</div>

<hr>

<div class="row">
  <div class="col-xs-4 col-sm-3 col-lg-2">
    {{ui-buttons
      type='radio'
      buttons='Foo,Bar,Baz'
    }}
  </div>
  <div class="col-xs-4 col-sm-4 col-lg-5 monospace">
  <b>\{{ui-buttons </b><br/>
  &nbsp;&nbsp;buttons='Foo,Bar,Baz' <br/>
  &nbsp;&nbsp;type='radio'<br/>
  }}
  </div>
  <div class="col-xs-4 col-sm-4 col-lg-5">
    This should produce similar results but uses the more compact "inline" form. The only thing to pay attention to right now is that
  </div>
</div>
<hr>

<br/>

<h3>Managing at Group Level</h3>
<hr>
<div class="row">
  <div class="col-xs-5 col-sm-4 col-lg-3">
    {{ui-buttons
      type='radio'
      buttons='Foo,Bar,Baz'
      disabled='baz'
      moodActive='success'
      defaultValue='bar'
    }}
  </div>
  <div class="col-xs-3 col-sm-3 col-lg-4 monospace">
  <b>\{{ui-buttons </b><br/>
  &nbsp;&nbsp;buttons='Foo,Bar,Baz' <br/>
  &nbsp;&nbsp;type='radio'<br/>
  &nbsp;&nbsp;disabled='baz'<br/>
  &nbsp;&nbsp;moodActive='success'<br/>
  &nbsp;&nbsp;defaultValue='bar'<br/>
  }}
  </div>
  <div class="col-xs-4 col-sm-4 col-lg-5">
    Here we see the more compact yet highly capable inline form of the component. A things to take note of:
    <ul>
      <li>
        <b>CSV Loading</b> - for many properties which can take on multiple values you can obviously bind to an array in the container but you may also use a CSV string in the template to load the values. Here you see that in <code>buttons</code> and <code>disabled</code>. Also note, in the case of <i>buttons</i> the CSV input sets the 'title' as the literal string passed in but the 'value' is a <i>dasherized</i> version of the string.
      </li>
      <li><b>Global Properties</b> - properties like <code>moodActive</code> (as well as <code>size</code> and others)</li>
    </ul>
    More on these properties later ...
  </div>
</div>
<hr>
<div class="row">
  <div class="col-xs-5 col-sm-4 col-lg-3">
    {{ui-buttons
      type='radio'
      buttons=dynButtons
      size=size
      moodActive='warning'
      defaultValue='baz'
      canBeEmpty=canBeEmpty
    }}
  </div>
  <div class="col-xs-3 col-sm-3 col-lg-4 monospace">
  <b>\{{ui-buttons </b><br/>
  &nbsp;&nbsp;buttons='{{dynButtons}}' <br/>
  &nbsp;&nbsp;type='radio'<br/>
  &nbsp;&nbsp;moodActive='success'<br/>
  &nbsp;&nbsp;defaultValue='bar'<br/>{{#if size}}
  &nbsp;&nbsp;size='{{size}}'<br/>{{/if}}{{#if canBeEmpty}}
  &nbsp;&nbsp;canBeEmpty=true<br/>{{/if}}
  <b>}}</b>
  </div>
  <div class="col-xs-4 col-sm-4 col-lg-5">
    Now play around with a few parameters:
    <div class="row">
    <div class="col-xs-3">buttons</div>
      <div class="col-xs-9">
        {{text-input value=dynButtons }}
      </div><br/>
      <div class="col-xs-3 downer">size</div>
      <div class="col-xs-9 downer">
        {{ui-buttons buttons=sizes size='small' type='radio' value=size }}
      </div><br/>
      <div class="col-xs-3 downer">canBeEmpty</div>
      <div class="col-xs-9 downer">
        {{ui-buttons
          buttons=canBeEmptyValues
          value=canBeEmpty
          size='small'
          type='radio'
          defaultValue=true
          icon=icon
          iconActive=iconActive
          iconInactive=iconInactive
          canBeEmpty=false
        }}
      </div><br/>
      <div class="col-xs-3 downer">icons</div>
      <div class="col-xs-9 downer">
        {{ui-buttons
          buttons='none,both,active,inactive'
          value=iconStrategy
          size='small'
          type='radio'
          defaultValue='none'
          canBeEmpty=false
        }}
      </div><br/>
    </div>
  </div>
</div>

<br/>

<h3>Data Down, Actions Up</h3>
<hr>
<div class="row">
  <div class="col-xs-5 col-sm-4 col-lg-3">
    {{ui-buttons
      type='radio'
      buttons='Foo,Bar,Baz'
      defaultValue='bar'
      value=listenDontTalk
      changed="changed"
    }}<br/>
    <p class="downer">value: {{listenDontTalk}}</p>  </div>
  <div class="col-xs-3 col-sm-3 col-lg-4 monospace">
  <b>\{{ui-buttons </b><br/>
  &nbsp;&nbsp;buttons='Foo,Bar,Baz' <br/>
  &nbsp;&nbsp;type='radio'<br/>
  &nbsp;&nbsp;defaultValue='bar'<br/>
  &nbsp;&nbsp;value=listenDontTalk<br/>
  &nbsp;&nbsp;changed="changed"<br/>
  }}
  </div>
  <div class="col-xs-4 col-sm-4 col-lg-5">
    We're trying to use the now accepted norm of "data down, actions up" to avoid side-effects and therefore when you use the <code>ui-buttons</code> components you can bind input values such as <i>size</i>,
  </div>
</div>